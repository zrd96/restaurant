#ifndef _TOOSL_H
#define _TOOSL_H

#include <string>
#include <vector>

using namespace std;

string ntos(int i);
string ntos(double f);
string getTime();
void viewResultList(vector<vector<string> >);

#endif
